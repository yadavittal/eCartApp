
import { createContext } from "react";

export const apple = createContext({
    dataName: "Hello"
})
// we can use it in api also insted of calling api every time we can store it in context hub
// const name=createContect('vittahl) 